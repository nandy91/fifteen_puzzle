15 PUZZLE

A web page enabled by Javascript that lets user to play the game of 15. Used a 4-by-4 board (implemented as an HTML table) for a total of 16 slots. One of the slots is empty. The aim is to order the random board by moving a tile at a time into the empty slot.